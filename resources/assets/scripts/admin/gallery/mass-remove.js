export const massRemove = () => {
    const massRemoveBtn = document.querySelector('.a-gallery__mass-remove');

    massRemoveBtn.addEventListener('click', () => {
        let newText = massRemoveBtn.getAttribute('data-cancel');

        // massRemoveBtn.
    })
}
